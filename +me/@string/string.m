classdef string
    methods (Static)
        [str] = camelcase(str,varargin)
        [str] = textline(title, varargin)
        [text] = replacemarks(text,varargin)
        [tf] = isversion(str,varargin)
        [str] = compactfloat(value)
    end
end

